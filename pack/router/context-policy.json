{
  "version": 6,
  "mode": "strict-routed-context",
  "runtime": "node+npm",
  "principles": {
    "default_deny_repository_reads": true,
    "lazy_load_skills": true,
    "lazy_expand_code_context": true,
    "prefer_index_before_source": true,
    "route_by_navigation_question_type": true,
    "prefer_targeted_search_for_direct_lookup": true,
    "prefer_graphify_for_relationship_discovery_when_fresh": true,
    "prefer_graphify_when_project_graph_ready": true,
    "require_graphify_freshness_before_use": true,
    "refresh_graphify_once_after_code_changing_step": true,
    "graphify_is_navigation_not_behavior_authority": true,
    "graphify_fallback_to_standard": true,
    "graphify_refresh_failure_never_blocks_task": true,
    "never_load_full_graphify_graph_into_context": true,
    "prefer_symbol_or_range_reads": true,
    "prefer_diff_after_implementation": true,
    "handoff_uses_summary_not_full_history": true,
    "original_prompt_always_available_to_captain": true,
    "web_kit_runtime_requires_node_not_python": true,
    "automatic_context_rollover_under_session_controller": true,
    "default_context_rollover_threshold_percent": 50,
    "rollover_only_at_safe_workflow_unit_boundary": true,
    "validate_handoff_before_fresh_provider_session": true,
    "fresh_session_verifies_handoff_against_repository": true,
    "controller_owns_context_reset_in_controlled_sessions": true
  },
  "repository_navigation": {
    "capability_source": ".agent-core/index/project-profile.json",
    "canonical_rule": ".agent-core/rules/repository-navigation.md",
    "graphify_refresh_gate": ".agent-core/rules/graphify-refresh.mjs",
    "graphify_state": ".agent-core/state/graphify.json",
    "graphify_refresh_gate_command": "node .agent-core/rules/graphify-refresh.mjs --project . --task-id <task-id>",
    "graphify_refresh_command": "graphify update .",
    "question_type_rule": "Choose navigation by the question being answered, not by tool availability alone. Exact text/symbol/path lookups use targeted current-source search first. Relationship, dependency, ownership, path-tracing, and impact questions prefer fresh Graphify when available, then verify exact source.",
    "direct_lookup": {
      "examples": [
        "where is this function defined",
        "find this exact error message",
        "where is this endpoint implemented",
        "find references to this exact symbol",
        "inspect the current implementation of this file"
      ],
      "preferred_progression": [
        "project-profile-when-needed",
        "rg-or-equivalent-targeted-current-source-search",
        "exact-file-or-symbol",
        "read-current-source"
      ],
      "rule": "Use rg when shell access and ripgrep are available; otherwise use the assistant's equivalent targeted current-source search. Do not force a Graphify query for a precise lookup just because a graph exists."
    },
    "relationship_discovery": {
      "examples": [
        "what calls this function",
        "what depends on this service",
        "what components are connected to this API",
        "what could be affected by changing this interface",
        "trace this endpoint through service to database",
        "which modules are related to this feature"
      ],
      "preferred_progression": [
        "project-profile",
        "graphify-refresh-gate",
        "narrow-graphify-query-path-or-neighbors",
        "small-candidate-symbol-set",
        "verify-exact-source-symbol-or-range",
        "targeted-search-for-gaps",
        "evidence-backed-expansion"
      ],
      "rule": "Prefer Graphify only when the graph is ready, the freshness gate passes, routing_mode is graphify-assisted, and dirty is false. Otherwise immediately use standard targeted repository navigation."
    },
    "standard": [
      "project-profile",
      "repository-index",
      "targeted-search",
      "symbol-or-range",
      "full-file-only-when-needed",
      "evidence-backed-expansion"
    ],
    "graphify-assisted": [
      "project-profile",
      "graphify-refresh-gate",
      "narrow-graphify-query-path-or-neighbors",
      "small-candidate-symbol-set",
      "verify-exact-source-symbol-or-range",
      "targeted-search-for-gaps",
      "full-file-only-when-needed",
      "evidence-backed-expansion"
    ],
    "graphify_runtime_rule": "Before the first Graphify use in a task, run the Node-based managed Graph Refresh Gate. After every completed code-changing step, run it once before Handoff Validator or another agent uses Graphify. The gate skips Graphify when repository state is unchanged and runs one incremental graphify update . only when relevant repository state changed.",
    "graphify_fallback_rule": "If Graphify refresh/query is unavailable, fails, times out, is stale/incomplete, or conflicts with source, record task-local standard fallback and continue without blocking the task. Do not repeatedly retry the same failed capability in one task unless explicitly requested.",
    "source_authority_rule": "Graphify answers where to look. Targeted search and exact source answer what currently exists. Current repository source, current diff, tests/build checks, and runtime evidence remain authoritative over Graphify and structural indexes.",
    "mental_model": {
      "graphify": "Where should I look?",
      "targeted_search_and_source": "What actually exists?",
      "diff": "What changed?",
      "tests_build_runtime": "Does the relevant behavior actually work?"
    }
  },
  "session_control": {
    "enabled_when_launched_by_controller": true,
    "controller": ".agent-core/bin/session-controller.mjs",
    "canonical_rule": ".agent-core/rules/context-rollover.md",
    "control_agent": "context-rollover-manager",
    "supported_providers": ["codex", "claude"],
    "default_rollover_threshold_percent": 50,
    "threshold_min_percent": 10,
    "threshold_max_percent": 90,
    "safe_boundary": "Evaluate rollover only after the active provider finishes one safe Web-Kit workflow unit and persists session-progress.json. Do not intentionally interrupt a provider in the middle of an edit/tool call.",
    "progress_state": ".agent-core/state/session-progress.json",
    "controller_state": ".agent-core/state/session-controller.json",
    "handoff_state": ".agent-core/state/context-handoff.json",
    "handoff_archive": ".agent-core/state/handoffs/",
    "handoff_rule": "Before creating a fresh provider session, validate a compact handoff containing original request, workflow progress, decisions/constraints, changed/validated scope, repository snapshot, exact next action, rollover reason, and context telemetry provenance.",
    "fresh_context_rule": "The first cycle of a fresh provider session reads the pending handoff before broad discovery, verifies material claims against current source/diff/tests/runtime, reconciles mismatches, and resumes the exact recorded next action.",
    "authority_rule": "Context handoff is routing/state evidence only. Current source, current diff, relevant tests/build, and runtime evidence override the handoff.",
    "provider_process_rule": "The controller uses provider structured/headless modes. Below threshold it resumes the current provider session; at/above threshold it launches a genuinely fresh provider process/session and injects the validated handoff. It does not emulate /clear or /new terminal keystrokes.",
    "controlled_ai_rule": "When WEB_KIT_SESSION_CONTROLLER=1, the AI must not run /clear, /new, /compact, or ask the user to reset context. It completes one safe workflow unit and writes session-progress.json; the controller owns rollover.",
    "telemetry_rule": "Use provider-reported current context occupancy when available. Do not use cumulative lifetime token spend as current context percentage. If telemetry remains unavailable for the configured fallback cycle count, use a conservative telemetry-unavailable-safety rollover and record that it was not an exact threshold measurement.",
    "done_rule": "session-progress status=done is valid only after the original request passes the canonical workflow's required final validation.",
    "blocked_rule": "status=blocked stops the controller and preserves state only for a genuine user/permission dependency."
  },
  "runtime_policy": {
    "required": ["node", "npm"],
    "not_required": ["python", "pip", "pipx"],
    "graphify_optional": true,
    "graphify_python_managed_by_uv": true,
    "graphify_setup": ".agent-core/rules/graphify-setup.mjs",
    "graphify_completion_command": "node .agent-core/rules/graphify-setup.mjs --project . --complete",
    "session_controller_requires_provider_cli": true,
    "session_controller_provider_clis": ["codex", "claude"]
  },
  "task_sizes": {
    "small": {
      "candidate_files_initial": 5,
      "candidate_files_hard_cap": 12,
      "dependency_depth_initial": 1,
      "approx_context_tokens_per_worker": 7000,
      "max_active_skills": 3
    },
    "medium": {
      "candidate_files_initial": 10,
      "candidate_files_hard_cap": 25,
      "dependency_depth_initial": 2,
      "approx_context_tokens_per_worker": 14000,
      "max_active_skills": 5
    },
    "large": {
      "candidate_files_initial": 15,
      "candidate_files_hard_cap": 40,
      "dependency_depth_initial": 2,
      "approx_context_tokens_per_worker": 24000,
      "max_active_skills": 7
    }
  },
  "expansion_rules": [
    "Expand context only when current source or a task-relevant fresh Graphify relationship identifies a dependency, symbol, contract, test, configuration, or ownership boundary that is necessary to complete or validate the task.",
    "Every expansion must record why the additional file/symbol is needed.",
    "Do not recursively read every import/dependency or expand every graph neighbor.",
    "Do not run broad repository search before Graphify when the current question is primarily relationship/impact discovery and Graphify is fresh; use the graph to narrow first.",
    "Do not force Graphify before an exact text/symbol/path lookup; targeted current-source search is cheaper and more direct for that question type.",
    "Do not read generated, vendor, build, coverage, dependency-cache, Graphify output, or binary output unless the task explicitly targets it.",
    "If the hard candidate-file cap would be exceeded, return to the Captain for a scope/context review before reading more.",
    "Do not solve context pressure by forwarding the full prior transcript to a fresh session; use the validated compact handoff and re-read only evidence required for correctness."
  ],
  "excluded_paths": [
    ".git/**",
    ".agent-core/**",
    ".agents/**",
    "node_modules/**",
    "vendor/**",
    "bin/**",
    "obj/**",
    ".next/**",
    "dist/**",
    "build/**",
    "coverage/**",
    ".cache/**",
    ".idea/**",
    ".vscode/**",
    "graphify-out/**"
  ]
}
